import { LoginForm } from "@/features/auth/components/login-form";

export default function LoginPage() {
  return (
    <main className="min-h-screen bg-gray-600 flex items-center justify-center p-6">
      <LoginForm />
    </main>
  );
}
